parameters:
  - name: variables
    type: object
    default: []

variables:
- name: test
  value: declared_locally

steps:
  - ${{ each item in parameters.variables }}:
    - task: Bash@3
      displayName: use ${{ item }} variable
      inputs:
        targetType: 'inline'
        script: |
          echo "${{ item }}"

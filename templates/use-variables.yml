parameters:
  - name: variables
    type: object
    default: []

steps:
  - ${{ each item in parameters.variables }}:
    - task: Bash@3
      name: use_variable
      inputs:
        targetType: 'inline'
        script: |
          echo "${{ item }}"

parameters:
  - name: variables
    type: object
    default: []

steps:
  - ${{ each item in parameters.variables }}:
    - task: Bash@3
      variables:
        test: test
      displayName: use ${{ item }} variable
      inputs:
        targetType: 'inline'
        script: |
          echo "${{ item }}"
